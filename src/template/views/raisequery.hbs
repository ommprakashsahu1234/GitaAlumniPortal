<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../public/css/bootstrap.min.css"> 
    <link rel="stylesheet" href="../public/css/styles.css"> 
    <title>Raise Query</title>
    <style>

        .form-container {
            background-color: rgba(0, 0, 0, 0.7);
            padding: 30px;
            border-radius: 10px;
            border: 2px solid #ddd;
            max-width: 600px;
            margin: auto;
        }

        .form-container h2 {
            color: yellow;
            text-align: center;
        }

        .form-control {
            background-color: rgba(255, 255, 255, 0.9);
            color: black;
            border-radius: 5px;
        }

        .btn-submit {
            width: 100%;
        }
    </style>
</head>

<body>
    {{>mainheader}}

    <div class="container mt-5">
        <div class="form-container">
            <h2>Raise Your Query</h2>
            <form action="/raisequery" method="POST">
                <div class="mb-3">
                    <label for="query" class="form-label">Your Query</label>
                    <textarea class="form-control" id="query" name="query" rows="5" placeholder="Enter your query here..." required></textarea>
                </div>
                <button type="submit" class="btn btn-success btn-submit">Submit</button>
                <div id="messages">
                    {{#if success}}
                    <div class="alert alert-success">{{success}}</div>
                    {{/if}}
                    {{#if warn}}
                    <div class="alert alert-warning">{{warn}}</div>
                    {{/if}}
                </div>
            </form>
        </div>
    </div>

    {{>footer}}

    <script src="../public/js/bootstrap.bundle.js"></script> 
</body>

</html>

<script>
    if (window.history.replaceState) {
        window.history.replaceState(null, null, window.location.href);
    }
</script>
