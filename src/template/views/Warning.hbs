<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Use local CSS files for Bootstrap and custom styles -->
    <link rel="stylesheet" href="../public/css/bootstrap.min.css"> <!-- Bootstrap 5 CSS -->
    <link rel="stylesheet" href="../public/css/styles.css"> <!-- Your custom styles (if any) -->
    <title>Dashboard</title>
    <style>
        .table-section {
            margin-bottom: 2rem;
        }

        h2 {
            color: rgb(255, 255, 255);
        }

        .scrollable-container {
            overflow-y: auto;
            background-color: #212529;
            border: 1px solid #444;
            border-radius: 8px;
            padding: 20px;
            margin-top: 20px;
            margin-bottom: 20px;
        }

        .placeholder-item {
            margin-bottom: 15px;
            padding: 15px;
            background-color: #343a40;
            border-radius: 8px;
            color: #fff;
        }

        span {
            font-weight: bold;
            font-size: 3rem;
            background: linear-gradient(to right, #ff0000, #ff7f7f);
            -webkit-background-clip: text;
            background-clip: text;
            text-fill-color: transparent;
            text-shadow: 0 0 10px rgba(255, 0, 0, 0.7), 0 0 20px rgba(255, 0, 0, 0.7), 0 0 30px rgba(255, 0, 0, 0.7);
            
            
            
           
        }
    </style>
</head>
<script src="../public/js/bootstrap.bundle.js"></script>

<body>

    
    {{>mainheader}}

    <!-- Dashboard Content -->
    <span class="display-1 fw-bold d-flex justify-content-center align-items-center min-vh-100 text-center">
        {{warn}}
    </span>

    

    <!-- Bootstrap 5 JS file -->
    {{>footer}}

</body>
<script>
    function viewui(ui) {
        if (ui) {
            alert(ui)
        }
    }
</script>

</html>